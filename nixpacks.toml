[phases.install]
cmds = ["pnpm install --no-frozen-lockfile --prod=false"]

[phases.build]
cmds = [
  "pnpm --filter @workspace/dental-dashboard run build",
  "pnpm --filter @workspace/api-server run build"
]
